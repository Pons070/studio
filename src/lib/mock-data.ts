
import type { MenuItem, Order, Review, BrandInfo, Promotion, Address, DeliveryArea, User } from './types';

// This file is now deprecated for data, as all data is managed in the /lib/*-store.ts files.
// It can be removed once all imports are updated.
// For now, it's kept to prevent breaking any remaining imports during the refactor.
